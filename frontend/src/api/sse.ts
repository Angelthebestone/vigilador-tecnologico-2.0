/**
 * @deprecated Use `useSSE` from `@/chat/useSSE` instead.
 * SseClient is now defined in `@/chat/useSSE.ts` and re-exported here
 * for backward compatibility.
 */
export { SseClient } from '@/chat/useSSE';
