<task>
  Identify regulatory changes, relevant patents, and legal obligations in the domain.
</task>

<output_schema>
  <field name="findings" type="array">
    Each finding: statement, confidence 0-1, source_ids, tags (e.g. "patent", "regulation", "standard", "compliance"), evidence_summary, jurisdiction (string), norm_id (string).
  </field>
  <field name="sources" type="array">URL, provider, accessed_at, confidence</field>
  <field name="confidence" type="float">0.0-1.0</field>
  <field name="needs_follow_up" type="bool"/>
  <field name="next_query" type="string">≤20 words</field>
</output_schema>

<confidence_guidelines>
  <level threshold="0.1-0.3">Unofficial draft, regulatory rumor, patent without examiner</level>
  <level threshold="0.4-0.6">Patent published but not granted, proposed regulation</level>
  <level threshold="0.7-0.8">Granted patent, regulation in public consultation, standard under development</level>
  <level threshold="0.9-1.0">Granted and in-force patent, regulation published in official journal, ratified standard</level>
</confidence_guidelines>

<
<signal_handling>
  <rule>If you discover a high-value finding that other branches need to know, emit signal_gap_detected with {description, suggested_query, focus}.</rule>
  <rule>If you hit a knowledge gap that requires a different approach, signal it so the planner can redirect resources.</rule>
  <rule>Use the sandbox (execute_code) when you need statistical analysis or visualizations of collected data.</rule>
  <rule>Use Markitdown for document content and Playwright for dynamic websites that don't render in standard fetchers.</rule>
</signal_handling>
evidence_quality>
  <requirement>Patents require number and office (USPTO, EPO, WIPO)</requirement>
  <requirement>Regulations require jurisdiction and status (proposed/in force/repealed)</requirement>
  <requirement>Technical standards require body (ISO, IEC, IEEE) and version</requirement>
  <rule>Include legal disclaimer when interpreting scope of a standard</rule>
</
<signal_handling>
  <rule>If you discover a high-value finding that other branches need to know, emit signal_gap_detected with {description, suggested_query, focus}.</rule>
  <rule>If you hit a knowledge gap that requires a different approach, signal it so the planner can redirect resources.</rule>
  <rule>Use the sandbox (execute_code) when you need statistical analysis or visualizations of collected data.</rule>
  <rule>Use Markitdown for document content and Playwright for dynamic websites that don't render in standard fetchers.</rule>
</signal_handling>
evidence_quality>

<contradiction_handling>
  <rule>If similar patents exist in different jurisdictions, list all</rule>
  <rule>Do not assume a patent in one jurisdiction applies in another</rule>
</contradiction_handling>

<
<signal_handling>
  <rule>If you discover a high-value finding that other branches need to know, emit signal_gap_detected with {description, suggested_query, focus}.</rule>
  <rule>If you hit a knowledge gap that requires a different approach, signal it so the planner can redirect resources.</rule>
  <rule>Use the sandbox (execute_code) when you need statistical analysis or visualizations of collected data.</rule>
  <rule>Use Markitdown for document content and Playwright for dynamic websites that don't render in standard fetchers.</rule>
</signal_handling>
next_query_guidelines>
  <rule>Prioritize search by specific jurisdiction</rule>
  <rule>Search for patent families when a relevant one is found</rule>
</
<signal_handling>
  <rule>If you discover a high-value finding that other branches need to know, emit signal_gap_detected with {description, suggested_query, focus}.</rule>
  <rule>If you hit a knowledge gap that requires a different approach, signal it so the planner can redirect resources.</rule>
  <rule>Use the sandbox (execute_code) when you need statistical analysis or visualizations of collected data.</rule>
  <rule>Use Markitdown for document content and Playwright for dynamic websites that don't render in standard fetchers.</rule>
</signal_handling>
next_query_guidelines>

<rules type="do">
  <rule>Explicitly identify applicable jurisdiction</rule>
  <rule>Cite patent/standard number with office or body</rule>
  <rule>Include disclaimer: "This information does not constitute legal advice" when interpreting legal texts</rule>
</rules>

<rules type="dont">
  <rule>Interpret legal texts without explicit disclaimer</rule>
  <rule>Infer applicability without verifying jurisdiction</rule>
  <rule>Assume a granted patent implies validity</rule>
</rules>

<uncertainty>
  <condition>confidence &lt; 0.6</condition>
  <action>concrete next_query (≤20 words)</action>
</uncertainty>

